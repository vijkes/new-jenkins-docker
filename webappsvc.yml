apiVersion: v1
kind: Service
metadata:
    name: my-service
    labels:
      app: myapp-service
spec:
    selector:
       app: mynewjavaapp
    type: NodePort
    ports:
    - nodePort: 30001
      port: 80
      targetPort: 80
